function calcAverageSpeed(distance, time) {
    let averageSpeed = distance / time;

    console.log(averageSpeed.toFixed(2));
}

calcAverageSpeed(200, 2);
calcAverageSpeed(150, 3);
calcAverageSpeed(300, 5);